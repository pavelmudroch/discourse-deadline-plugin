en:
    site_settings:
        deadline_enabled: 'Enable deadline plugin'
        allow_deadline_on_all_categories: 'Allow deadline plugin on all categories'
        allow_deadline_on_categories: 'Select categories where deadline plugin is allowed. `allow_deadline_on_all_categories` must be false.'
        auto_bump_topic_on_deadline_day: 'Automatically bump topic on deadline day'
        deadline_far_color: 'Text color for deadline far (format: #rrggbb | #rgb)'
        deadline_far_background_color: 'Background color for deadline far (format: #rrggbb | #rgb)'
        deadline_soon_days: 'Number of days before deadline to mark as `deadline-soon`'
        deadline_soon_color: 'Text color for deadline soon (format: #rrggbb | #rgb)'
        deadline_soon_background_color: 'Background color for deadline soon (format: #rrggbb | #rgb)'
        deadline_today_color: 'Text color for deadline today (format: #rrggbb | #rgb)'
        deadline_today_background_color: 'Background color for deadline today (format: #rrggbb | #rgb)'
        deadline_expired_color: 'Text color for deadline expired (format: #rrggbb | #rgb)'
        deadline_expired_background_color: 'Background color for deadline expired (format: #rrggbb | #rgb)'
