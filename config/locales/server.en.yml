en:
    site_settings:
        deadline_enabled: 'Enable deadline plugin'
        deadline_allowed_on_categories: 'Select categories where deadline plugin is allowed. Leave blank to allow on all categories.'
        deadline_auto_bump_topic: 'Automatically bump topic on each day less than threshold'
        deadline_auto_bump_topic_threshold: 'Number of days before deadline to bump topic'
        deadline_soon_days_threshold: 'Number of days before deadline to mark as `deadline-soon`'
